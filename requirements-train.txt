# Training dependencies — run locally to generate model files
# Requires Java 11 (e.g. Eclipse Adoptium JDK 11)
pyspark==3.4.3
scikit-learn>=1.4.0
pandas>=2.1.0
numpy>=1.26.0
pyarrow>=15.0.0
fastparquet>=2023.10.0
